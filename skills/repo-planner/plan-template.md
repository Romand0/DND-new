# <功能名称> — 执行计划

> 创建时间：<YYYY-MM-DD>
> 基于 Profile：<.repo/profile.md 的更新日期>
> 复杂度：简单 / 中等 / 复杂
> 涉及文件数：<N>

---

## 需求摘要

<2-3 句话描述要做什么，解决什么问题>

## 影响面分析

### 调用链

```
<从入口到数据层的完整调用路径>
例：
  DicePage.tsx handleRoll()
    → setHistory()  ← 这里改显示逻辑
    → broadcastDiceResult()
      → diceService listeners
```

### 变更文件清单

| # | 文件路径 | 变更类型 | 改动描述 |
|---|---------|---------|---------|
| 1 | <path> | 新增 / 修改 / 删除 | <具体改什么> |

---

## 详细方案

### 文件 1：<file_path>

**变更类型**：修改 / 新增 / 删除

**改动位置**：第 X-Y 行 / `<函数名>` 内

**改动描述**：
<具体描述改什么、为什么这样改>

**改前**：
```ts
// <当前代码>
```

**改后**：
```ts
// <目标代码>
```

**注意事项**：
- <容易踩的坑>
- <与其他模块的交互影响>

---

### 文件 2：<file_path>

<同上格式>

---

## 依赖关系图

```
任务依赖（无环）：
  A（无依赖）──→ C（依赖 A）──→ D（依赖 B+C）
  B（无依赖）──↗
```

### 可并行组

| 组 | 任务 | 可并行原因 |
|----|------|-----------|
| A | <文件 1, 文件 2> | 互不 import |
| B | <文件 3> | 独立模块 |
| C | <文件 4> | 依赖 A 的接口签名 |

---

## 风险点

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| <风险描述> | <影响范围> | <如何应对> |

## 决策点（需用户确认）

> 仅在有多种实现路径且各有取舍时列出。完全明确的方案不需要此节。

| # | 决策 | 选项 A | 选项 B | 推荐 |
|---|------|--------|--------|------|
| 1 | <问题> | <方案 A + 利弊> | <方案 B + 利弊> | A/B |

## 验证步骤

| 步骤 | 命令 | 预期结果 |
|------|------|---------|
| 1 | `<cmd>` | <具体预期> |
| 2 | `<cmd>` | <具体预期> |

## Code Agent 执行指引

> 给执行本方案的 code agent 的提示。

1. **执行前**：先读 <列几个关键文件> 确认当前代码状态与方案描述一致
2. **执行顺序**：按依赖图从底层开始（A → B → C → D）
3. **每步验证**：每完成一组文件后跑 `<type-check cmd>`
4. **完成后**：跑 `<full build cmd>` 确认整体通过
5. **遇到问题**：如果实际代码与方案描述不一致（函数签名变了/文件已重构），停下来报告，不要硬套方案
