# 流程数据架构重构规划

## 一、重构背景

当前系统采用三层复杂的数据架构，存在数据存储分散、状态管理混乱、用户体验不佳等问题。为了提升系统架构的清晰度、数据一致性和用户体验，我们计划进行流程数据架构的重构，从三层架构简化为两层架构。

## 二、当前架构问题

### 现有三层架构
1. **LocalStorage临时态**：草稿的临时存储
2. **flow_drafts持久态**：草稿的持久化存储
3. **flow发布态**：已发布流程的存储

### 存在的问题
- 数据存储分散，难以统一管理
- 临时态和持久态区分复杂，增加系统复杂度
- 数据同步困难，容易出现不一致
- 用户需要担心数据丢失，体验不佳
- 架构不符合现代应用的最佳实践

## 三、重构目标

### 核心理念
简化数据架构，提升用户体验，确保数据安全，为未来发展奠定基础。

### 目标架构
**两层简洁架构**：
1. **flow_drafts（草稿态）**：沙盒环境，自由编辑的工作空间
2. **flow（发布态）**：静态的已发布流程，生产环境使用

## 四、新架构设计

### 第一层：flow_drafts（草稿态）

#### 定位与特性
- **沙盒环境**：自由编辑的工作空间
- **实时同步**：任何编辑操作都会实时回传到后端数据库
- **无临时态区分**：取消"临时态"和"持久态"的复杂区分
- **编辑友好**：为用户提供流畅的编辑体验

#### 核心价值
- 编辑操作的即时反馈
- 数据安全的保障
- 编辑体验的优化

### 第二层：flow（发布态）

#### 定位与特性
- **静态稳定**：只读的、稳定的发布版本
- **生产环境**：真正被编译器执行并应用到战斗场景
- **不可变**：发布后保持静态，不被直接修改

#### 核心价值
- 生产环境的稳定性
- 执行结果的可靠性
- 版本管理的清晰性

## 五、流程生命周期设计

### 1. 创建阶段
- **操作**：在流程库创建新流程
- **结果**：flow_drafts 表自动生成初始化流程草稿
- **特点**：即创建即可编辑，无需等待

### 2. 编辑阶段
- **操作**：在 FlowEditor 中进行流程编辑
- **机制**：所有变更实时回写到 flow_drafts 表
- **特点**：无延迟、无丢失、实时同步

### 3. 发布阶段
- **操作**：点击发布按钮进行流程发布
- **验证**：合法性检查（不通过不能发布）
- **数据迁移**：从 flow_drafts 拷贝到 flow 表
- **结果**：流程进入发布态，可供生产环境使用

### 4. 重复编辑
- **操作**：点击已发布流程的"编辑"按钮
- **行为**：自动回到沙盒环境（flow_drafts）
- **状态**：发布态（flow）保持静态不变
- **继续**：编辑 flow_drafts，发布后覆写 flow 表

### 5. 同步编辑
- **操作**：清空草稿，与发布版本同步
- **机制**：用 flow 表覆写 flow_drafts 表
- **用途**：确保草稿与发布版本保持一致

### 6. 撤下阶段
- **操作**：撤下已发布的流程
- **结果**：从 flow 表删除发布态，保留 flow_drafts 中的草稿态
- **价值**：保留编辑历史，支持重新发布

### 7. 删除阶段
- **操作**：彻底删除流程
- **结果**：同步删除 flow 和 flow_drafts 表中的数据
- **特点**：彻底清理，不留冗余数据

## 六、重构价值

### 用户体验价值
- **实时编辑**：编辑操作即时保存，无需担心数据丢失
- **流畅体验**：简化操作流程，减少用户认知负担
- **数据安全**：所有操作实时持久化，保障数据安全

### 技术架构价值
- **架构清晰**：从三层复杂架构简化为两层简洁架构
- **数据一致**：实时同步机制确保前后端数据一致性
- **扩展性强**：为未来多用户协作、版本控制等功能奠定基础

### 业务发展价值
- **效率提升**：减少数据同步的复杂性，提升开发效率
- **维护成本降低**：简化架构，降低长期维护成本
- **技术现代化**：从localStorage存储升级到数据库驱动，符合现代应用架构

## 七、实施原则

### 渐进式重构
- 保持现有功能的连续性
- 分阶段实施，降低风险
- 充分测试，确保质量

### 用户体验优先
- 确保重构过程中用户体验不受影响
- 提供清晰的操作指引
- 及时收集用户反馈

### 数据安全第一
- 做好数据备份和迁移
- 确保数据完整性
- 建立应急回滚机制

## 八、预期成果

### 短期成果
- 用户体验显著提升
- 数据安全性增强
- 系统架构简化

### 长期成果
- 技术债务清理
- 系统扩展性大幅提升
- 为未来发展奠定坚实基础

## 九、总结

本次流程数据架构重构是一次战略性升级，从复杂的三层架构简化为清晰的两层架构。通过实时同步机制和清晰的数据分离，我们不仅能够提升用户体验，还能为系统的长期发展奠定坚实基础。这是一次高回报的技术投资，将对整个系统的健康产生深远影响。
