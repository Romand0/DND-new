# 流程发布错误修复待办清单

## 📋 问题概述

DSL编译器（FlowEditor.tsx）的草稿即使通过验证检查，仍然无法发布/更新，并且弹出"错误：500"，修改会被保存但是不能上传。

## 🔍 根因分析

### 主要问题
1. **API客户端缺失** - `src/lib/api.ts` 中缺少完整的flow相关API客户端函数
2. **数据链断裂** - `flowStore.publish` 直接使用 `fetch` 而不是统一的 `apiFetch` 函数
3. **验证逻辑不完善** - 验证标准与实际发布需求不匹配
4. **后端数据序列化问题** - 双重序列化可能导致数据丢失或格式问题

## 🎯 修复任务清单

### 任务一：补充API客户端函数
**目标**：在 `src/lib/api.ts` 中添加完整的flow相关API客户端函数

**具体步骤**：
1. 备份当前仓库状态
2. 在 `src/lib/api.ts` 中添加以下函数：
   - `fetchAllFlows<T = any[]>(): Promise<T>`
   - `createFlow<T = any>(data: T): Promise<T>`
   - `updateFlow<T = any>(id: string, data: T): Promise<T>`
   - `deleteFlow(id: string): Promise<void>`
   - `publishFlow<T = any>(id: string): Promise<T>`
3. 确保所有函数使用统一的 `apiFetch` 函数
4. 运行类型检查：`npx tsc --noEmit`
5. 构建项目：`npm run build`
6. 验证无语法错误且可以正常部署

### 任务二：修复flowStore的publish方法
**目标**：统一使用 `apiFetch` 函数，确保数据链完整性

**具体步骤**：
1. 备份当前仓库状态
2. 修改 `src/data/flowStore.ts` 中的 `publish` 方法
3. 将直接 `fetch` 调用替换为 `apiFetch` 调用
4. 确保认证头和错误处理的一致性
5. 运行类型检查：`npx tsc --noEmit`
6. 构建项目：`npm run build`
7. 验证无语法错误且可以正常部署

### 任务三：完善验证逻辑
**目标**：确保发布前的验证更加严格和一致

**具体步骤**：
1. 备份当前仓库状态
2. 修改 `src/pages/flow-editor/validation.ts` 中的验证逻辑
3. 统一草稿和已发布流程的验证标准
4. 增强数据状态转换的一致性检查
5. 运行类型检查：`npx tsc --noEmit`
6. 构建项目：`npm run build`
7. 验证无语法错误且可以正常部署

### 任务四：优化后端数据序列化
**目标**：避免双重序列化，确保数据完整性

**具体步骤**：
1. 备份当前仓库状态
2. 修改 `functions/api/flows/[id].ts` 中的数据序列化逻辑
3. 移除不必要的双重序列化
4. 确保数据格式的一致性
5. 运行类型检查：`npx tsc --noEmit`
6. 构建项目：`npm run build`
7. 验证无语法错误且可以正常部署

## 🔄 工作流程规范

### 每次任务前的备份流程
```bash
# 1. 检查当前状态
git status
git log --oneline -5

# 2. 创建备份分支
git checkout -b backup-fix-flow-publish-$(date +%Y%m%d-%H%M%S)

# 3. 推送备份到远程（可选）
git push origin backup-fix-flow-publish-$(date +%Y%m%d-%H%M%S)

# 4. 记录备份信息
echo "备份分支: backup-fix-flow-publish-$(date +%Y%m%d-%H%M%S)"
echo "备份时间: $(date)"
echo "备份前提交: $(git rev-parse HEAD)"
```

### 每次任务后的验证流程
```bash
# 1. 类型检查
npx tsc --noEmit

# 2. 构建项目
npm run build

# 3. 检查构建结果
ls -la dist/

# 4. 提交当前任务完成
git add .
git commit -m "fix: 完成任务X - 修复流程发布问题"

# 5. 查看状态
git status
git log --oneline -3
```

## 📝 注意事项

1. **任务顺序**：严格按照任务一到任务四的顺序执行，不要跳过或重新排序
2. **备份重要性**：每次开始新任务前必须备份，确保可以回滚
3. **验证要求**：每个任务完成后必须进行类型检查和构建，确保无语法错误
4. **停止等待**：每个任务完成后立即停止，等待用户确认后再开始下一任务
5. **错误处理**：如果任何步骤失败，立即停止并报告问题

## 🚨 风险控制

- **数据丢失风险**：通过Git备份确保可以回滚
- **语法错误风险**：通过类型检查和构建验证确保代码质量
- **部署失败风险**：通过逐步修复和验证降低风险
- **回归风险**：通过任务化修复确保每个变更都是可控的

## 📞 问题报告

如果在执行过程中遇到任何问题，请立即报告：
- 具体的错误信息
- 执行的步骤和命令
- 仓库的当前状态
- 需要的帮助类型

---

**创建时间**：2026年1月23日  
**创建者**：AI Coding Agent  
**状态**：待开始  
**下一个任务**：等待用户指令启动任务一